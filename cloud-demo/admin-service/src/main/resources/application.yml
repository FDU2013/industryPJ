server:
  port: 8093
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cloud_admin?useSSL=false
    username: root
    password: 1234zjx
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  type-aliases-package: cn.itcast.user.pojo
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    cn.itcast: debug
  pattern:
    dateformat: MM-dd HH:mm:ss:SSS